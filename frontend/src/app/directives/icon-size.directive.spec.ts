import { IconSizeDirective } from './icon-size.directive';

describe('IconSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new IconSizeDirective();
    expect(directive).toBeTruthy();
  });
});
